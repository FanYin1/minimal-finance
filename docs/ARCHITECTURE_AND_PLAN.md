# Puhui 企业级前端开发框架

## 📋 项目概述

**项目名称**: Puhui  
**项目类型**: 企业官网前端开发框架  
**技术栈**: Next.js 14 (App Router) + TypeScript + Tailwind CSS  
**部署方式**: GitHub Actions CI/CD + Vercel/自托管

---

## 🏗️ 技术架构

### 架构模式：元框架模式 (Meta-Framework Pattern)

```
┌─────────────────────────────────────────────────────────────────┐
│                        Puhui Framework                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Pages     │  │  Components │  │   Hooks     │             │
│  │  (App Dir)  │  │  (Reusable) │  │  (Logic)    │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │   Styles    │  │   Utils     │  │   Types     │             │
│  │ (Tailwind)  │  │  (Helpers)  │  │(TypeScript) │             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
├─────────────────────────────────────────────────────────────────┤
│                     Next.js App Router                          │
├─────────────────────────────────────────────────────────────────┤
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────┐             │
│  │    SSR      │  │    SSG      │  │    ISR      │             │
│  │  (Server)   │  │  (Static)   │  │(Incremental)│             │
│  └─────────────┘  └─────────────┘  └─────────────┘             │
├─────────────────────────────────────────────────────────────────┤
│                    React 18 + Node.js                           │
└─────────────────────────────────────────────────────────────────┘
```

### 核心技术选型

| 技术领域 | 选型方案 | 版本 | 选型理由 |
|---------|---------|------|---------|
| 框架 | Next.js | 14.x | App Router、RSC支持、性能优化 |
| 语言 | TypeScript | 5.x | 类型安全、开发体验、可维护性 |
| 样式 | Tailwind CSS | 3.x | 原子化CSS、快速开发、一致性 |
| 状态管理 | Zustand | 4.x | 轻量级、TypeScript友好 |
| HTTP客户端 | Axios | 1.x | 拦截器、错误处理 |
| 代码规范 | ESLint + Prettier | - | 代码质量、格式统一 |
| 测试 | Jest + React Testing Library | - | 单元测试、组件测试 |
| CI/CD | GitHub Actions | - | 自动化构建部署 |

---

## 📁 项目目录结构

```
Puhui/
├── .github/                    # GitHub 配置
│   └── workflows/
│       ├── ci.yml              # 持续集成工作流
│       └── deploy.yml          # 部署工作流
├── app/                        # Next.js App Router 页面
│   ├── (marketing)/            # 营销页面分组
│   │   ├── page.tsx            # 首页
│   │   ├── about/
│   │   │   └── page.tsx        # 关于我们
│   │   ├── products/
│   │   │   └── page.tsx        # 产品服务
│   │   ├── cases/
│   │   │   └── page.tsx        # 案例展示
│   │   ├── news/
│   │   │   ├── page.tsx        # 新闻列表
│   │   │   └── [id]/
│   │   │       └── page.tsx    # 新闻详情
│   │   └── contact/
│   │       └── page.tsx        # 联系我们
│   ├── api/                    # API 路由
│   │   └── contact/
│   │       └── route.ts        # 联系表单API
│   ├── globals.css             # 全局样式
│   ├── layout.tsx              # 根布局
│   └── not-found.tsx           # 404页面
├── components/                 # 可复用组件
│   ├── layout/                 # 布局组件
│   │   ├── Header.tsx          # 页头导航
│   │   ├── Footer.tsx          # 页脚
│   │   └── Container.tsx       # 容器组件
│   ├── ui/                     # UI 基础组件
│   │   ├── Button.tsx          # 按钮
│   │   ├── Card.tsx            # 卡片
│   │   ├── Input.tsx           # 输入框
│   │   └── Modal.tsx           # 弹窗
│   ├── sections/               # 页面区块组件
│   │   ├── Hero.tsx            # 首页Banner
│   │   ├── Features.tsx        # 特性展示
│   │   ├── Testimonials.tsx    # 客户评价
│   │   └── CTA.tsx             # 行动召唤
│   └── common/                 # 通用组件
│       ├── Logo.tsx            # Logo组件
│       ├── SEO.tsx             # SEO组件
│       └── Loading.tsx         # 加载组件
├── hooks/                      # 自定义 Hooks
│   ├── useScroll.ts            # 滚动Hook
│   ├── useMediaQuery.ts        # 响应式Hook
│   └── useForm.ts              # 表单Hook
├── lib/                        # 工具库
│   ├── api.ts                  # API 封装
│   ├── utils.ts                # 工具函数
│   └── constants.ts            # 常量定义
├── types/                      # TypeScript 类型定义
│   ├── index.ts                # 类型导出
│   └── api.ts                  # API 类型
├── public/                     # 静态资源
│   ├── images/                 # 图片资源
│   ├── icons/                  # 图标资源
│   └── fonts/                  # 字体资源
├── styles/                     # 样式文件
│   └── components.css          # 组件样式
├── .env.example                # 环境变量示例
├── .env.local                  # 本地环境变量
├── .eslintrc.json              # ESLint 配置
├── .prettierrc                 # Prettier 配置
├── .gitignore                  # Git 忽略文件
├── next.config.js              # Next.js 配置
├── package.json                # 项目依赖
├── postcss.config.js           # PostCSS 配置
├── tailwind.config.ts          # Tailwind 配置
├── tsconfig.json               # TypeScript 配置
└── README.md                   # 项目说明
```

---

## 🚀 开发流程

### 1. 环境准备

```bash
# 要求
Node.js >= 18.17.0
npm >= 9.0.0 或 pnpm >= 8.0.0
Git >= 2.0.0
```

### 2. 项目初始化

```bash
# 克隆项目
git clone https://github.com/your-org/Puhui.git
cd Puhui

# 安装依赖
npm install

# 复制环境变量
cp .env.example .env.local

# 启动开发服务器
npm run dev
```

### 3. 开发命令

```bash
npm run dev          # 启动开发服务器 (localhost:3000)
npm run build        # 构建生产版本
npm run start        # 启动生产服务器
npm run lint         # 代码检查
npm run lint:fix     # 自动修复代码问题
npm run format       # 格式化代码
npm run test         # 运行测试
npm run test:watch   # 监听模式测试
npm run type-check   # TypeScript 类型检查
```

---

## 🔄 GitHub CI/CD 工作流

### 持续集成流程 (CI)

```yaml
触发条件: Push 到任意分支 / Pull Request
执行步骤:
  1. 代码检出
  2. Node.js 环境配置
  3. 依赖安装 (带缓存)
  4. 代码检查 (ESLint)
  5. 类型检查 (TypeScript)
  6. 单元测试 (Jest)
  7. 构建测试
```

### 持续部署流程 (CD)

```yaml
触发条件: Push 到 main 分支
执行步骤:
  1. CI 流程
  2. 构建生产版本
  3. 部署到目标环境
  4. 健康检查
  5. 通知团队
```

### 分支策略

```
main          ← 生产分支 (自动部署到生产环境)
  │
  ├── develop ← 开发分支 (自动部署到测试环境)
  │     │
  │     ├── feature/xxx  ← 功能分支
  │     ├── fix/xxx      ← 修复分支
  │     └── refactor/xxx ← 重构分支
```

---

## 📝 开发规范

### 命名规范

| 类型 | 规范 | 示例 |
|-----|------|------|
| 组件文件 | PascalCase | `Button.tsx`, `UserProfile.tsx` |
| 工具文件 | camelCase | `formatDate.ts`, `apiClient.ts` |
| 样式类名 | kebab-case | `hero-section`, `nav-link` |
| 常量 | UPPER_SNAKE_CASE | `API_BASE_URL`, `MAX_ITEMS` |
| 类型/接口 | PascalCase + I前缀 | `IUser`, `IApiResponse` |

### 组件开发规范

```tsx
// 组件模板示例
'use client'; // 仅客户端组件需要

import { FC, ReactNode } from 'react';
import { cn } from '@/lib/utils';

interface IButtonProps {
  children: ReactNode;
  variant?: 'primary' | 'secondary' | 'outline';
  size?: 'sm' | 'md' | 'lg';
  className?: string;
  onClick?: () => void;
}

export const Button: FC<IButtonProps> = ({
  children,
  variant = 'primary',
  size = 'md',
  className,
  onClick,
}) => {
  return (
    <button
      className={cn(
        'rounded-lg font-medium transition-colors',
        // variant styles
        variant === 'primary' && 'bg-primary text-white hover:bg-primary/90',
        variant === 'secondary' && 'bg-secondary text-white hover:bg-secondary/90',
        variant === 'outline' && 'border border-primary text-primary hover:bg-primary/10',
        // size styles
        size === 'sm' && 'px-3 py-1.5 text-sm',
        size === 'md' && 'px-4 py-2 text-base',
        size === 'lg' && 'px-6 py-3 text-lg',
        className
      )}
      onClick={onClick}
    >
      {children}
    </button>
  );
};
```

### Git 提交规范

```
<type>(<scope>): <subject>

类型(type):
  feat     - 新功能
  fix      - 修复 Bug
  docs     - 文档更新
  style    - 代码格式调整
  refactor - 代码重构
  perf     - 性能优化
  test     - 测试相关
  chore    - 构建/工具更新

示例:
  feat(header): 添加响应式导航菜单
  fix(api): 修复联系表单提交错误
  docs(readme): 更新安装说明
```

---

## 🎨 设计系统

### 颜色规范

```css
/* 主题色 */
--primary: #2563EB;        /* 主色 - 蓝色 */
--primary-dark: #1D4ED8;   /* 主色深色 */
--secondary: #10B981;      /* 辅助色 - 绿色 */

/* 中性色 */
--gray-50: #F9FAFB;
--gray-100: #F3F4F6;
--gray-200: #E5E7EB;
--gray-300: #D1D5DB;
--gray-400: #9CA3AF;
--gray-500: #6B7280;
--gray-600: #4B5563;
--gray-700: #374151;
--gray-800: #1F2937;
--gray-900: #111827;

/* 语义色 */
--success: #10B981;
--warning: #F59E0B;
--error: #EF4444;
--info: #3B82F6;
```

### 字体规范

```css
/* 字体家族 */
font-family: 'Inter', 'PingFang SC', 'Microsoft YaHei', sans-serif;

/* 字号 */
--text-xs: 0.75rem;    /* 12px */
--text-sm: 0.875rem;   /* 14px */
--text-base: 1rem;     /* 16px */
--text-lg: 1.125rem;   /* 18px */
--text-xl: 1.25rem;    /* 20px */
--text-2xl: 1.5rem;    /* 24px */
--text-3xl: 1.875rem;  /* 30px */
--text-4xl: 2.25rem;   /* 36px */
--text-5xl: 3rem;      /* 48px */
```

### 间距规范

```css
/* 间距 (基于 4px) */
--spacing-1: 0.25rem;  /* 4px */
--spacing-2: 0.5rem;   /* 8px */
--spacing-3: 0.75rem;  /* 12px */
--spacing-4: 1rem;     /* 16px */
--spacing-5: 1.25rem;  /* 20px */
--spacing-6: 1.5rem;   /* 24px */
--spacing-8: 2rem;     /* 32px */
--spacing-10: 2.5rem;  /* 40px */
--spacing-12: 3rem;    /* 48px */
--spacing-16: 4rem;    /* 64px */
--spacing-20: 5rem;    /* 80px */
```

---

## 📊 性能优化策略

### 图片优化
- 使用 Next.js Image 组件
- WebP 格式优先
- 响应式图片 srcset
- 懒加载 (loading="lazy")

### 代码分割
- 动态导入 (dynamic import)
- 路由级别代码分割
- 组件级别懒加载

### 缓存策略
- 静态资源 CDN 缓存
- API 响应缓存
- ISR 增量静态再生成

### SEO 优化
- 服务端渲染 (SSR)
- 元数据优化
- 结构化数据
- Sitemap 生成

---

## 🔒 安全最佳实践

1. **环境变量管理**: 敏感信息使用 `.env` 文件，不提交到仓库
2. **输入验证**: 所有用户输入进行验证和消毒
3. **HTTPS**: 生产环境强制 HTTPS
4. **CSP**: 配置内容安全策略
5. **依赖安全**: 定期更新依赖，使用 `npm audit`

---

## 📚 参考资源

- [Next.js 官方文档](https://nextjs.org/docs)
- [Tailwind CSS 文档](https://tailwindcss.com/docs)
- [TypeScript 手册](https://www.typescriptlang.org/docs)
- [React 文档](https://react.dev)
- [GitHub Actions 文档](https://docs.github.com/en/actions)

---

## 📞 技术支持

如有问题，请联系开发团队或提交 Issue。

**文档版本**: v1.0.0  
**最后更新**: 2026-01-11
